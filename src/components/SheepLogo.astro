---
interface Props {
  size?: number;
  class?: string;
}

const { size = 40, class: className = '' } = Astro.props;
---

<img
  class={className}
  src="/sheep-hd.png"
  width={size}
  height={size}
  alt="OviNas"
  role="img"
  aria-label="OviNas"
  draggable="false"
/>
